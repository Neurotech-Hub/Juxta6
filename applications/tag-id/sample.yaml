sample:
  name: Tag Identity
  description: >
    HIL: log hwinfo device ID, BLE identity address, JX_XXXXXX name, and
    mobile profile. LED1 green brief on boot.

common:
  tags:
    - tag
    - bringup
    - identity

tests:
  applications.tag-id:
    platform_allow:
      - nrf54l15tag/nrf54l15/cpuapp
    harness: console
