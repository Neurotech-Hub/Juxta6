# Phase 0 / HIL: LED1 RGB cycle + button log on nRF54L15 Tag
CONFIG_GPIO=y

CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_LOG_MAX_LEVEL=4

# RTT console comes from --snippet rtt-console / nRF Connect RTT Console snippet
CONFIG_MAIN_STACK_SIZE=2048
