# Juxta5-8 BLE peripheral: GATT LED, battery SAADC read, magnet-wake sleep
CONFIG_GPIO=y
CONFIG_ADC=y

# SAADC gates analog front-end via runtime PM inside each adc_read (see adc_nrfx_saadc).
CONFIG_PM_DEVICE=y
CONFIG_PM_DEVICE_RUNTIME=y

CONFIG_HEAP_MEM_POOL_SIZE=4096

CONFIG_BT=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_DEVICE_NAME="Juxta5-8-BLE"
CONFIG_BT_DEVICE_APPEARANCE=0
CONFIG_BT_MAX_CONN=1
CONFIG_BT_HCI_ERR_TO_STR=y

# TX power: +8 dBm from board defconfig (Juxta5-8_nRF52840_defconfig).

# Logging over RTT
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_USE_SEGGER_RTT=y
CONFIG_RTT_CONSOLE=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=n

CONFIG_MAIN_STACK_SIZE=6144
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

# P0.09 is NFC; use as GPIO for LED0.
CONFIG_NFCT_PINS_AS_GPIOS=y

# sys_poweroff() for deep-sleep GATT path (nRF52 implements z_sys_poweroff).
CONFIG_POWEROFF=y
